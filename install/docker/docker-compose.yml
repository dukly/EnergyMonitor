services:
  energy-monitor-agent:
    build:
      context: ../..
      dockerfile: install/docker/Dockerfile
    image: energy-monitor-agent:local
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
