services:
  energy-monitor-agent:
    build:
      context: ../..
      dockerfile: install/docker/Dockerfile.agent
    env_file: .env
    volumes:
      - ./data:/data
      - ./logs:/logs
    restart: unless-stopped
    network_mode: host
